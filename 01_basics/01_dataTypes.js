"use strict"; // treats all js code as newer or latest version

//alert(3+3) // shows a popup with the value but we don't use it as much and 
            //also it's syntax has been changed in latest version
            //as we are using node js not a browser

console.log(3+3) ; console.log("swaraj") ;

let name = "swaraj"; // string data type
let age = 21; // number data type
let isApproved = false; // boolean data type
let firstName ;// undefined data type mtlb value abhi define nhi ki hai
let selectedColor = null; // null data type it is a standalone value
let Name = Symbol("swaraj");

console.log(typeof Name);z